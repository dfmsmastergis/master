const n1 = 5;
const n2 = 8;

if (n1 > n2) { alert(n1 + " es mayor que " + n2) };

if (n1 <= 0) { alert(n1 + " es menor o igual a cero") };

if (n1 + 1 < n2) { alert(n1 + " + 1 no es mayor o igual que " + n2) };